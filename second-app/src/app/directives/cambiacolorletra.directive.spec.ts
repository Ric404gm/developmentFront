import { CambiacolorletraDirective } from './cambiacolorletra.directive';

describe('CambiacolorletraDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiacolorletraDirective();
    expect(directive).toBeTruthy();
  });
});
