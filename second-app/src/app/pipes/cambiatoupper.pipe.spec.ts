import { CambiatoupperPipe } from './cambiatoupper.pipe';

describe('CambiatoupperPipe', () => {
  it('create an instance', () => {
    const pipe = new CambiatoupperPipe();
    expect(pipe).toBeTruthy();
  });
});
